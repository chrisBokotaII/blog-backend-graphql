const { Comment } = require("./Comment");
const { Post } = require("./Post");
const { Query } = require("./Query");
const { User } = require("./User");
const { Mutation } = require("./Mutation");

module.exports = { Comment, Post, Query, User, Mutation };
